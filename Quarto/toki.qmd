---
title: "<br />トキの分布をしらべる<br />&nbsp;<br />&nbsp;"
author: "伊東宏樹"
date: 2026-05-30
lang: ja
format:
  revealjs:
    theme: [default, custom.scss]
    code-copy: true
    center-title-slide: false
embed-resources: true
slide-number: true
---

## はじめに

2026年5月31日、羽咋市においてトキ野生復帰（石川県能登地域）の放鳥が実施されます

![トキ (© Marco Mussita https://www.inaturalist.org/photos/77371650)](https://inaturalist-open-data.s3.amazonaws.com/photos/77371650/medium.jpeg){fig-align="center"}

## 本日の内容

::: incremental
- Kanazawa.R #1での発表「[Rで生物多様性情報を取得する](https://ito4303.github.io/KanazawaR001/rgbif.html)」では、コウノトリの出現 (Occurrence) 情報を地図にプロット

- 今回は[**トキ**]{style="color: red;"}

- GBIFから出現データを取得

- 内容を確認

- 位置を地図表示
:::

## GBIFとは

- GBIF [(Global Biodiversity](https://www.gbif.org/) [Information](https://www.gbif.org/ja/) [Facility](https://www.gbif.org/)、じーびふ)

::: {style="font-size: 85%;"}
> GBIF－地球規模生物多様性情報機構は、地球上のあらゆる種類の生物に関するデータを誰でも、どこにでも、オープンアクセスで提供することを目的として、世界中の政府から資金提供されて設置された国際的なネットワークであり、データ基盤です。
:::

::: {style="text-align: right; font-size: 85%;"}
（[GBIFとは何か？](https://www.gbif.org/ja/what-is-gbif)）
:::

## rgbifパッケージ

- Rからは[rgbif](https://cran.r-project.org/package=rgbif)パッケージによりデータを取得できる

- 処理の流れ

  1.  種 (species) データの取得

  2.  種を条件に指定して、出現（occurrence) データをダウンロード

```{r}
#| label: setup
#| echo: false

library(rgbif)
```

## 種 (species) のデータ取得 {.r-output}

- rgbifパッケージの`name_backbone()`関数で、トキの学名*Nipponia nippon*を指定して種のデータを取得

  - GBIFのアカウントが必要。rgbifの設定は [Set Up Your GBIF Username and Password](https://docs.ropensci.org/rgbif/articles/gbif_credentials.html) を参照

```{r}
#| label: name_backbone
#| echo: true

species_data <- name_backbone("Nipponia nippon") # トキの学名
species_data |>
  dplyr::select(scientificName, usageKey)
```

## ダウンロード情報の取得

- `occ_download()`関数でダウンロードを要求
  - `pred()`関数で`taxonKey`にトキのusageKeyを指定

```{r}
#| label: occ_download
#| echo: true
#| eval: false

occ_info <- occ_download(pred("taxonKey",
                              species_data$usageKey))
```

## ダウンロードしたデータを読み込み

1.  `occ_download_wait()`でダウンロード可能になるのを待つ
2.  `occ_download_get()`でデータをダウンロード
3.  `occ_download_import`関数で、ダウンロードしたデータを読み込みむ

```{r}
#| label: gbif_download_get
#| echo: true
#| eval: false

occ_download_wait(occ_info)
x <- occ_download_get(occ_info)
df <- occ_download_import(x)
```

```{r}
#| label: gbif_download_import
#| echo: false

# 2回目以降はダウンロード済みのファイルがあるのでそちらを読み込む
download_file <- "0012446-260507073636908.zip"
df <- occ_download_import(as.download(download_file))
```

## データの確認

[Darwin Core](https://gbif.jp/datause/dataformat/)形式。特定の変数のみ表示。

```{r}
#| label: view-data-run
#| echo: false

df |>
  dplyr::select(eventDate, countryCode, locality) |>
  DT::datatable(
    options = list(
      # フォントの調整
      initComplete = DT::JS(
        "function(settings, json) {",
        "$(this.api().table().container()).css({'font-size': '18px'});",
        "}"
      )
    )
  )
```

## 国別の頻度分布 {.r-output}

トキの出現情報のある国の頻度分布を表示

```{r}
#| label: country

df |>
  dplyr::pull(countryCode) |>
  table()
```

- やはり中国(CN)が多い
- NG（ナイジェリア）やPG（パプアニューギニア）というデータも。
  - 何かの間違いらしい。

## 地図データの準備

- データから、観察データかつ観察国が中国・日本・韓国・台湾であるものを残し、緯度経度座標のないものを除外

```{r}
#| label: filter-location
#| echo: true

df_loc <- df |>
  dplyr::filter(basisOfRecord == "HUMAN_OBSERVATION",
                countryCode %in% c("CN", "JP", "KR", "TW")) |>
  dplyr::filter_out(is.na(decimalLatitude) |
                    is.na(decimalLongitude))
  # filter_out は dplyr 1.2.0以降
```

## 地図の生成

- 地図表示のためleafletパッケージを使用

```{r}
#| label: map-asia
#| echo: true
#| message: false

library(leaflet)
labs <- paste(htmltools::htmlEscape(df_loc$eventDate),
              "<br />",
              htmltools::htmlEscape(df_loc$locality))
toki_map <- leaflet(df_loc) |>
  addTiles() |>
  setView(lng = 125, lat = 35, zoom = 4) |>
  addCircleMarkers(~decimalLongitude, ~decimalLatitude,
                   color = "red", radius = 2,
                   popup = labs)
```

## 東アジアにおけるトキの観察場所

```{r}
toki_map
```
